<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:layout_gravity="bottom"
    android:background="@drawable/overlay_background">

    <!-- Imagen del Asistente -->
    <ImageView
        android:id="@+id/assistantOverlayImage"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/cronoshi"
        android:layout_alignParentStart="true"
        android:rotationY="180"
        android:contentDescription="Asistente Virtual" />

    <!-- Texto del diálogo -->
    <TextView
        android:id="@+id/overlayDialogText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/assistantOverlayImage"
        android:layout_marginStart="8dp"
        android:text="Bienvenido al museo..."
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:gravity="center_vertical"
        android:padding="8dp"
        android:background="@drawable/dialog_background"
        android:textColor="@color/black" />
</RelativeLayout>